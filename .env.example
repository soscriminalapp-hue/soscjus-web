# URL do backend Fastify (produção). Inclui o prefixo /api/v1 — todas as rotas
# do backend ficam sob /api/v1 (o app mobile usa a mesma base). Sem o /api/v1
# a web bate 404 em todas as chamadas.
NEXT_PUBLIC_API_URL=https://api.soscriminalapp.com.br/api/v1
